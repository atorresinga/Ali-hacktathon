import React from "react";
import ReactDOM from "react-dom/client";
import RuralPricingApp from "./RuralPricingApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <RuralPricingApp />
  </React.StrictMode>
);
