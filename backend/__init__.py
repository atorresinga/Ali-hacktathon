"""
BACKEND PACKAGE — FastAPI HTTP API for the Andean potato GMML advisory tool.

- App factory / ASGI app: `backend.main:app`
- Versioned routes under `/api/v1`
- Localization (es / qu / ay) and governance endpoints live here

Domain logic (SQLite, ETL, stats): `andean_potato`
"""
